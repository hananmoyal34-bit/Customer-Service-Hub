import React from 'react';

// This component is no longer used as the product datasheet has been removed.
// Product selection is now handled by a simple text input.
const ProductSelector: React.FC = () => {
    return null;
};

export default ProductSelector;
